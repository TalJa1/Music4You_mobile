import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const LearnTabView = () => {
  return (
    <View>
      <Text>LearnTabView</Text>
    </View>
  );
};

export default LearnTabView;

const styles = StyleSheet.create({});
