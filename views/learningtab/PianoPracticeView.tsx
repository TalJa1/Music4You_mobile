import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const PianoPracticeView = () => {
  return (
    <View>
      <Text>PianoPracticeView</Text>
    </View>
  );
};

export default PianoPracticeView;

const styles = StyleSheet.create({});
